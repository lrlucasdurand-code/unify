useunify.io, www.useunify.io {
    # Backend API (preserve /api prefix)
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Frontend
    handle {
        reverse_proxy frontend:3000
    }
}
